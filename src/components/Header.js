import logo from "../images/logo.jpg";

function Header() {
  return (
    <header class="head">
      <img class="logo" src={logo} alt="Litle lemon logo" />
    </header>
  );
}

export default Header;
